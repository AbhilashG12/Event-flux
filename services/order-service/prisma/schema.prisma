generator client {
  provider   = "prisma-client-js"
  output   = "../generated/prisma"

}

datasource db {
  provider = "postgresql"
 
}

model Order {
  id        String   @id
  userId    String
  amount    Float
  status    String
  createdAt DateTime @default(now())
}